
logging:
    level:
        ROOT: INFO
        io.github.villcab.spring-registry: INFO

spring:
    application:
        name: SpringRegistry

server:
    port: 8761

eureka:
    client:
        register-with-eureka: false
        fetch-registry: false
